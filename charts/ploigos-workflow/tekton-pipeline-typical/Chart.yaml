apiVersion: v2
name: ploigos-workflow-tekton-pipeline-typical
description: |
  A Helm chart for Kubernetes to install the Ploigos Workflow (Typical) run by Tekton for a
  given application service.
type: application
maintainers:
  - name: Red Hat, Inc.
    email: ploigos@redhat.com
dependencies:
  - name: ploigos-workflow-tekton-resources
    version: ">= 0.0.0 || >= 0.0.0-0"
# MANAGED BY: GitHub workflow
version: 0.18.0
