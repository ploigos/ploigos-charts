apiVersion: v2
name: ploigos-workflow-jenkins-cluster-resources
description: |
  A Helm chart for Kubernetes to deploy the cluster level resources required to
  deploy and run Ploigos Workflows using Jenkins.
type: application
maintainers:
  - name: Red Hat, Inc.
    email: ploigos@redhat.com
dependencies:
  - name: ploigos-workflow-shared-cluster-resources
    version: ">= 0.0.0 || >= 0.0.0-0"
# MANAGED BY: GitHub workflow
version: 0.19.0-edge.2
